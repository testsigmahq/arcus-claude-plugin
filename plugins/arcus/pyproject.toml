[project]
name = "arcus-scripts"
version = "0.2.0"
requires-python = ">=3.9"
dependencies = [
    "keyring>=24.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-mock>=3.12",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["scripts"]
