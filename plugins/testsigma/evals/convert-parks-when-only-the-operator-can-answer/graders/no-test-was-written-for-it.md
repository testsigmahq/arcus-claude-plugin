---
type: regex
pattern: '\.sigma'
target: files
match: not_contains
---
The half of parking that a guess would satisfy.

The reason-code helper types a value and never submits, and whether selecting a
reason code commits the change is a fact about the application under test that
neither the source nor the target project answers. A model with a deadline
resolves that by picking the reading that lets it finish, and the test it writes
looks finished and is wrong in a way no check downstream can see.
