# Residue

Not final. A cause is overturned when a spelling is found or the format gains one.

| Source Step | Element | Cause | Reasoning | Revisit when |
|---|---|---|---|---|
| `I refresh until the record appears` | `journal-filter` | unresolved element | The filter input is present in the page object but the existing Testsigma project has no matching screen, and nobody has captured it. | An element for the printing journal filter exists in the project |
