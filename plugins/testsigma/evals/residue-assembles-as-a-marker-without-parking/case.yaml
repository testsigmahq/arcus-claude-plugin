schema_version: "1.1"
name: residue-assembles-as-a-marker-without-parking
context:
  # The workspace starts empty and add_dirs only grants read access, so
  # the fixture suite is copied in. Settled by running it.
  scaffold_script: scaffold.sh
